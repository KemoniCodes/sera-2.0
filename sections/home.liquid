<section class="home">
  <div class="hero">
    <h1 class="animated animatedFadeInUp fadeInUp">
      {{ settings['hero-txt'] }}
    </h1>
    <a href="{{settings.hero-btn-link}}">
      <button class="animated-1 animatedFadeInUp fadeInUp">
        {{ settings['hero-btn-txt'] }}
      </button>
    </a>
  </div>
  <div class="images animated-2 animatedFadeInUp fadeInUp">
    <div class="img-container">
      <div>
        <img loading="lazy" class="img-1" src="{{settings.image-1 | img_url: '320x320'}}">
        <h5 class="img-1-text">{{ settings['image-1-text'] }}</h5>
      </div>
      <div class="img-2-container">
        <img loading="lazy" class="img-2" src="{{settings.image-2 | img_url: '450x450'}}">
        <h5 class="img-2-text">{{ settings['image-2-text'] }}</h5>
      </div>
    </div>
  </div>

  <h5 id="atelier" style="display: none;">{{ settings['hero-subtext'] }}</h5>
  <img
    loading="lazy"
    src="{{ section.settings.background | img_url: '450x450' }}"
    style="display: none;"
    id="background"
  >
</section>

{% style %}
  /* .home {
      background-image: url({{ section.settings.background | img_url: '450x450' }}) !important;
      position: relative;
      background-size: cover;
      height: 100vh;
      background-position-x: -2rem;
      background-repeat: no-repeat;
  } */
{% endstyle %}

{% schema %}
{
  "name": "t:sections.home-hero.name",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "image_picker",
      "id": "background",
      "label": "Mobile Background Img"
    }
  ]
}
{% endschema %}
